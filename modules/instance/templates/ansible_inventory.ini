[${instance_group}]
%{ for index, instance_host in instance_hosts ~}
${instance_name}-server-${index+1} ansible_user=${ansible_user} ansible_host=${instance_host.network_interface.0.access_config.0.nat_ip} ansible_port=${ansible_port}
%{ endfor ~}
